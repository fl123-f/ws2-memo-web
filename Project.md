## リポジトリ情報
- リポジトリ: https://github.com/fl123-f/ws2-memo-web
- 最新コミット: db00dec (2025-12-17)

## ディレクトリ構成
- `index.html`  
  - メモ一覧と新規メモ入力フォームを表示するメイン画面
  - カスタムモーダル（ダイアログ）コンテナを含む

- `js/main.js`  
  - メモ一覧の取得、新規メモの保存処理、画面の表示更新、カテゴリ選択・検索・ページネーションなどを担当
  - モーダルマネージャーの初期化を含む

- `js/modal.js`  
  - カスタムモーダル（ダイアログ）システムを提供
  - `confirm()`, `alert()`, `custom()`, `showMemoDetail()` などのメソッドを実装
  - Promiseベースの非同期制御、キーボード操作（ESC）、アニメーション対応

- `js/memoUI.js`  
  - メモの表示や展開／折りたたみの UI 処理を担当

- `js/memoHandlers.js`  
  - 保存、編集、削除、検索、カテゴリ選択、ページ切り替えなどのイベントハンドラを設定
  - モーダルを使用した確認ダイアログと成功/エラーメッセージ表示

- `js/memoService.js`  
  - フロントエンドからバックエンド API への通信を担当  
  - `getMemos`, `saveMemo`, `updateMemo`, `deleteMemo`, `importMemos`, `exportMemos` などの関数を提供
  - エラーハンドリングや読み込み状態の管理も行う

- `api/get_memos.php`  
  - SQLite からメモ一覧を取得し JSON で返す  
  - ページング、カテゴリ絞り込み、検索キーワード対応

- `api/save_memo.php`  
  - 新しいメモを受け取り SQLite に保存  
  - 作成日時を自動生成し、成功時には新規 ID を返す

- `api/update_memo.php`  
  - 既存メモの更新を受け付ける  
  - タイトル・内容・カテゴリの更新および更新日時の記録

- `api/delete_memo.php`  
  - メモ削除を受け付ける  
  - ソフトデリート（is_deleted フラグ）またはハード削除に対応

- `api/import_export.php`  
  - JSON または CSV ファイルからメモを一括インポート  
  - 現在のフィルタ条件に基づきメモを JSON または CSV でエクスポート
  - 成功件数、スキップ件数、エラー情報を返す

- `data/memo.db`  
  - メモを保存する SQLite データベースファイル

- `css/style.css`  
  - アプリケーション全体のスタイル定義
  - モーダルコンポーネントのスタイルを含む

- `test_modal.html`  
  - モーダル機能のテストページ
  - 各種モーダルタイプの動作確認用

- `.gitignore`  
  - Git で無視するファイルの設定
  - データベースファイル、ログファイル、IDE設定などを除外

- `web/` ディレクトリ  
  - リモートリポジトリからマージされた元のファイル（重複）
  - 現在のアプリケーションでは使用されていない

## 現在実装済みの機能
- メモ一覧表示
  - SQLite `memos` テーブルから取得し、日付順に表示
  - 各メモはタイトル・内容（短縮表示）、日付、カテゴリ、編集・削除ボタン付き
  - 展開／折りたたみ機能で内容を切り替え可能
- メモ新規作成
  - タイトル・内容・カテゴリを入力して保存可能
  - 保存後、一覧は自動更新
- メモ編集
  - 一覧の ✏️ ボタンで編集モードに切替
  - 保存ボタンで更新し、一覧表示は自動で更新
- メモ削除
  - 一覧の削除ボタンで削除可能
  - 削除時はカスタムモーダルで確認を表示
  - 削除後、一覧は自動更新
- カテゴリ選択
  - 左側カテゴリリストで選択すると、一覧が絞り込み表示
  - 選択カテゴリは太字表示でハイライト
- キーワード検索
  - タイトルおよび内容でのリアルタイム検索
- ページネーション
  - 一覧が多い場合、ページ切り替えボタンで表示切替
- カスタムモーダルシステム
  - ネイティブ `confirm()` ダイアログを置き換える統一されたUI
  - 以下のモーダルタイプを提供：
    - 確認モーダル（はい/いいえ）
    - 警告モーダル（OKのみ）
    - メモ詳細表示モーダル
    - カスタムHTMLコンテンツモーダル
  - アニメーション付きの表示/非表示
  - キーボード操作対応（ESCでキャンセル）
  - Promiseベースの非同期API
- 日本語ローカライゼーション
  - すべてのUIテキストを日本語に統一
  - コードコメントも日本語で記述
- テスト環境
  - モーダル機能のテスト用ページを提供

## 最近追加された機能（2025年12月）
- ✅ カスタムモーダルシステムの実装
- ✅ 日本語ローカライゼーション（UIテキストとコードコメント）
- ✅ GitHubリポジトリへのアップロードと同期
- ✅ モーダル機能テストページの作成

## 今後の改善・追加予定
- データのインポート／エクスポート機能の完全実装（JSON/CSV 対応）
- スマートフォンでも使いやすいレスポンシブ対応の強化
- メモ詳細画面の作成（個別メモの閲覧・編集）
- タグやカテゴリ管理機能の拡張
- メモ履歴・変更記録の実装（updated_at 記録、差分表示）
- エラーメッセージや成功通知の統一（トースト表示など）
- 検索・ページネーション・カテゴリ選択の連動改善
- ダークモード対応
- オフライン機能の実装（Service Worker）
- バックアップ/復元機能
